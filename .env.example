# Copy to .env and fill in real values (.env is git-ignored).
DB_USER=iris
DB_PASSWORD=change_me_locally
DB_NAME=irisdb
DB_HOST=localhost
DB_PORT=5433          # 5433 on host: native PostgreSQL 17 owns 5432 on this machine
API_KEY=dev-key-change-me
